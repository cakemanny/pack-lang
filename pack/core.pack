(ns pack.core)
;; Logic
(def not (fn [x] (if x false true)))

(import operator)
;; Arithmatic
(def + (. operator add))
(def - (. operator sub))
(def * (. operator mul))
(def / (. operator truediv))

;; vim:ft=clojure:
